{
    "type": "object",
    "default": {},
    "title": "Root Schema",
    "required": [
        "message"
    ],
    "properties": {
        "message": {
            "type": "string",
            "default": "",
            "title": "The message Schema",
            "examples": [
                "Registro alterado com sucesso"
            ]
        }
    },
    "examples": [{
        "message": "Registro alterado com sucesso"
    }]
}