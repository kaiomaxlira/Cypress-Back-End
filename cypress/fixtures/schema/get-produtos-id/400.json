{
    "type": "object",
    "default": {},
    "title": "Root Schema",
    "required": [
        "message"
    ],
    "properties": {
        "message": {
            "type": "string",
            "default": "",
            "title": "The message Schema",
            "examples": [
                "Produto não encontrado"
            ]
        }
    },
    "examples": [{
        "message": "Produto não encontrado"
    }]
}